<?php

use Illuminate\Database\Seeder;

/** This is a seeder for db website data.*/
class DatabaseDataSeeder extends Seeder
{
    /** Run the database seeds.
     * @return void */
    public function run()
    {
        //data tables
        if(env('APP_ENV') == 'local'){
            
        }
        
        if(env('APP_ENV') == 'local' || env('APP_ENV') == 'production'){
            
        }
        
    }
}
